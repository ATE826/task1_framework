# Task1 Framework

## Запуск

```bash
dotnet restore
dotnet run

# swagger

http://localhost:5000/swagger

Что нужно было сделать

Создать простую веб-службу (ASP.NET Core API) и показать, как работает конвейер обработки HTTP-запросов (middleware pipeline).


1. API (3 метода)
GET /api/items → список элементов
GET /api/items/{id} → один элемент или ошибка
POST /api/items → создание элемента


2. Хранение данных
без базы данных
всё хранится в памяти (List в сервисе)
⚙️ 3. Middleware (конвейер из 3 частей)

Нужно реализовать:

1) Логирование
запрос (метод, путь)
ответ (status code)
RequestId


2) Обработка ошибок
ловит исключения
возвращает единый JSON ошибки:
код ошибки
сообщение
RequestId


3) Performance middleware
измеряет время выполнения запроса
пишет в лог


4. Валидация

Минимум 2 правила:

имя не пустое
цена ≥ 0 (или аналогичные)


5. Проверки

Нужно показать:

можно создать и получить элемент
ошибка по несуществующему ID работает
лог содержит RequestId и время
сценарии можно воспроизвести через запросы
📄 6. Сдать
код (Git)
README с запуском
примеры запросов
короткое описание архитектуры